# Model backend: openai (direct OpenAI-compatible HTTP) or copilot
T2I_MODEL_BACKEND=openai

# GitHub Copilot SDK. Uses the signed-in Copilot user or the standard
# COPILOT_GITHUB_TOKEN/GH_TOKEN/GITHUB_TOKEN environment variables.
# OPENAI_* generation settings are ignored when this backend is selected.
# T2I_MODEL_BACKEND=copilot
# COPILOT_MODEL=auto
# COPILOT_REASONING_EFFORT=high
# COPILOT_OUTPUT_TOKEN_LIMIT=16384
# COPILOT_TIMEOUT_SECONDS=180

# Direct OpenAI-compatible author model
OPENAI_API_KEY=replace-with-your-api-key
OPENAI_MODEL=gpt-4.1-mini
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY_ENV=OPENAI_API_KEY
OPENAI_AUTH_MODE=bearer
OPENAI_STRUCTURED_OUTPUT_MODE=json_schema
OPENAI_TEMPERATURE=0.6
OPENAI_OUTPUT_TOKEN_LIMIT=16384
OPENAI_TIMEOUT_SECONDS=180
OPENAI_TRANSPORT_RETRIES=2

# Optional Theme semantic-duplication checks. A clean run uses one batched
# embedding request; each automatic Theme regeneration adds another audit.
# Later duplicate Themes are regenerated automatically.
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# OPENAI_EMBEDDING_DIMENSIONS=512
# THEME_SIMILARITY_SETTING_THRESHOLD=0.86

# Set only the reasoning control supported by the endpoint.
# OPENAI_REASONING_EFFORT=none
# OPENAI_THINKING_MODE=disabled

# Run policy is CLI-only: --concurrency 8 --theme-batch-size 5 --generation-retries 2

# Azure OpenAI example:
# AZURE_OPENAI_API_KEY=replace-with-your-azure-api-key
# OPENAI_BASE_URL=https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/
# OPENAI_API_KEY_ENV=AZURE_OPENAI_API_KEY
# OPENAI_AUTH_MODE=api_key
# OPENAI_MODEL=your-deployment-name
