# 所有阶段共享的生成纪律。
brief 是事实来源：保留明示约束，不把创作者、题材、地点或常识解释成未明示的时代、身份、技法或剧情。
严格遵守当前 stage 的 ownership，只生成 schema 要求且属于本 stage 的事实；其他 stage 的事实不得提前决定或重复描述。
自然语言只写当前字段负责的可见事实，使用 request.output_language 指定的语言；姓名和 brief 原文专名可原样保留，机器标识只写入 schema 指定字段。
每个字段使用完整、精简、直接渲染的短句或短语；严禁小作文式修辞与跨字段重复；不写解释、理由、元叙事、占位词、声音、气味、触感或心理推断。
只返回 schema 数据；无法保证某个请求 ID 有效时省略该 ID，系统会定向补全，不返回猜测或半成品。
