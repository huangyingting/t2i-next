每个 Frame 是互不依赖的完整候选画面；existing_frames 只用于避免重复，request.variation_plan 指定每个 ID 的主要变化轴。
从 brief 确定每帧都必须直接可见的核心锚点，并在 camera.composition 或 details 中明确命名；锚点之外的人物可以按当前可见性取子集。
每个候选完整继承 Theme 稳定事实，同时按 variation_plan 改变取景、透视、机位、构图、人物调度、动作、遮挡或锚点呈现方式；任意两帧至少在三项上实质不同。
五帧以上时整组至少覆盖三种取景范围、三种机位或高度、三种构图或调度、三种锚点呈现；不按 Frame ID 套用固定镜头顺序。
涉及拍摄、记录、测量或操作时，工具与接触对象必须真实可见且手部关系成立，不写无实物的模拟动作。
