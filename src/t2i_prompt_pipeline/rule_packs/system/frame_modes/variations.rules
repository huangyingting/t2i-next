每个 Frame 互不依赖，完整呈现 brief 全部可视事实；核心动作及工具、对象、颜色、数量和结果不得分散；existing_frames 只用于去重。
逐项落实 frame_visual_plan 的焦点、景别、机位、景深和光线策略，再选自然姿态；一致性优先，不改变核心活动凑变化。
五帧以上时，在上述约束内尽可能覆盖多种取景、机位、构图和锚点呈现；不按 Frame ID 套用固定镜头顺序。
涉及拍摄、记录、测量或操作时，工具与接触对象必须真实可见且手部关系成立，不写无实物的模拟动作。
