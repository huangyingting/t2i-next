每个 Frame 互不依赖，完整呈现 brief 全部可视事实；核心动作及其工具、对象、颜色、图案、数量和结果不得分散到其他 Frame；existing_frames 只用于去重，request.variation_plan 指定变化轴。
按 variation_plan 优先改变取景、机位和构图，再选自然姿态；一致性优先于差异数量，不用复杂手势或改变核心活动凑变化。
五帧以上时，在上述约束内尽可能覆盖多种取景、机位、构图和锚点呈现；不按 Frame ID 套用固定镜头顺序。
涉及拍摄、记录、测量或操作时，工具与接触对象必须真实可见且手部关系成立，不写无实物的模拟动作。
