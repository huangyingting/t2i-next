镜头按 ID 顺序形成一条完整可见因果链；existing_frames 是已完成的前段，只用于延续状态和规划剩余推进。
每个 Theme 独立完成 brief 的完整故事弧：首帧建立未完成状态，中间帧呈现导致变化的动作，终帧在 brief 核心动词的语义上限内给出明确结果；不依赖其他 Theme 补前因或结果。
人物屏幕方向、视线轴线、服饰状态、关键道具的位置、持有者和所在手连续；任何变化都必须由本组画面中的可见动作建立，不凭空出现、消失或改手。
brief 指定地点顺序、高度变化或搬运路线时，Frames 按顺序经过全部地点，并直接呈现必要连接与到达证据。
先确定终帧的合法结果，再反向选择每一帧不可缺少的推进；每帧都服务于核心行动，不用无关肖像或装饰特写填充。
